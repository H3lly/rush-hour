#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

#include "game.h"

struct game_s{
};

/*
bool can_move(piece p, dir d){
	switch(d){
		case RIGHT:
			if (movement_is_allowed(p,RIGHT)){
				piece tmp = malloc(sizeof(struct piece));	
				copy_piece(p, tmp);
				tmp -> abs += 1;
				for(int i=0;i<
					return !(intersect(tmp,v))
				}
			}
			return false;
	}
	
}

*/
